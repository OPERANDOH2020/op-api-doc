type: object
properties:
  sender:
    type: string
    description : Sender unique identifier
  to:
    description : Recipients user ids
    type: array
    items:
      type: string
  cc:
    description : Carbon Copy recipients user ids
    type: array
    items:
      type: string
  bcc:
    description : Blind Carbon Copy recipients user ids
    type: array
    items:
      type: string
  reply-to:	  
    description: Reply-to email address
    type:string	
  content:
    description: Email content
    type: string
  subject:
    description: Email subject
    type: string	
  attachments:
    description: Email attachments
    type: array
    items:
      $ref: 'https://raw.githubusercontent.com/OPERANDOH2020/op-api-doc/master/definitions/Attachment.yaml'