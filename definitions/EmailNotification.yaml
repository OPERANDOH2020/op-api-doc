type: object
properties:
  to:
    description :  user_id/email that should be notified
    type: array
    items:
      type: string
  cc:
    description : Copy Carbon user_id/email that should be notified
    type: array
    items:
      type: string
   bcc:
    description : Blind Copy Carbon user_id/email that should be notified
    type: array
    items:
      type: string
  content:
    description: EmailNotification content
    type: string
  subject:
    type: string
    description: Notification subject
  attachments:
    description: Notification attachments
    type: array
    items:
      $ref: 'https://raw.githubusercontent.com/OPERANDOH2020/op-api-doc/master/definitions/Attachment.yaml'