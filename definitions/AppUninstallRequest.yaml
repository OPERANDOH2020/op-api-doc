type: array
items:
  type: object
  properties:
    app_id:
      type: string
      description: App unique identifier
    uninstall_reason:
      type: string
      description: The reason why app is been recommended for uninstall